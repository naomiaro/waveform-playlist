import cursor from "states/cursor";
import fadein from "states/fadein";
import fadeout from "states/fadeout";
import record from "states/record";
import select from "states/select";
import shift from "states/shift";

//export an object holding all states.
export default {
	cursor,
	fadein,
	fadeout,
	record,
	select,
	shift
}
