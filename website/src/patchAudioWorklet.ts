// Intentionally empty — SAC global patching removed.
// SoundTouch integration uses rawContext.audioWorklet.addModule() for registration
// and context.createAudioWorkletNode() for node creation instead.
