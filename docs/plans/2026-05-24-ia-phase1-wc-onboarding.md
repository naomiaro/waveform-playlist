# IA Restructure — Phase 1: Web Components Onboarding Path

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Land the Web Components onboarding entry point. A first-time visitor at `/docs/` sees a framework-picker landing page, clicks the Web Components card, and reaches a working `<daw-editor>` quick start in under 10 minutes. The existing React onboarding (`/docs/getting-started/installation`) is unchanged.

**Architecture:** Adds a new `web-components/` directory under `website/docs/`. Docusaurus's autogenerated sidebar (`sidebars.ts: [{type: 'autogenerated', dirName: '.'}]`) picks up the new directory automatically — no `sidebars.ts` edits needed. The new `getting-started.md` is grounded in the existing `packages/dawcore/README.md` Quick Start content to avoid divergence between READMEs and site docs.

**Tech Stack:** Docusaurus 3.9 (MDX, autogenerated sidebars, broken-link checker), existing `packages/dawcore/README.md` as content source.

**Spec reference:** [`docs/specs/2026-05-24-ia-restructure-design.md`](../specs/2026-05-24-ia-restructure-design.md) — this plan implements the spec's Phase 1 ("Top-level scaffolding (sidebar + intro)") narrowed to the WC entry point only. Other scaffolding (Concepts shell, full React reorg, redirects framework) is deferred to subsequent phase plans.

---

## File Structure

**Create (2 files):**
- `website/docs/web-components/_category_.json` — Sidebar metadata (label, position, default link target)
- `website/docs/web-components/getting-started.md` — WC quick-start page

**Modify (1 file):**
- `website/docs/intro.md` — Full rewrite as a framework-picker landing page

**Deliberately untouched:**
- `website/sidebars.ts` — Autogenerated sidebar picks up the new directory automatically; no edit required
- `website/docs/getting-started/*.md` — Existing React onboarding stays intact (React-path users hit the same URLs they hit today)
- `website/docusaurus.config.ts` — No plugin install, no source alias for `@dawcore/*` needed (this plan ships only static markdown; live WC example components are a later phase)

---

## Task 1: Replace `intro.md` with framework-picker landing

**Files:**
- Modify: `website/docs/intro.md` (full rewrite)

- [ ] **Step 1: Confirm the current file contents** so the rewrite is intentional, not accidental

Run:
```bash
cat website/docs/intro.md
```

Expected: The current intro.md (Features list, Packages table, Quick Example, Browser Support, Next Steps). This entire file is being replaced.

- [ ] **Step 2: Replace the file with the new framework-picker content**

Write `website/docs/intro.md` with this exact content:

````markdown
---
sidebar_position: 1
slug: /
description: "Multitrack Web Audio editor — choose React components or framework-agnostic Web Components."
---

# Waveform Playlist

A multitrack Web Audio editor and player with canvas waveform visualization. One headless engine, two integration paths: React components or framework-agnostic Web Components.

## What it does

- **Multitrack mixing** — Load, arrange, and mix multiple audio tracks
- **Effects** — 20 Tone.js effects with real-time parameter control (React)
- **Recording** — Mic capture with live waveform preview, multi-channel support
- **MIDI** — Load `.mid` files; piano-roll rendering; SoundFont playback
- **Spectrogram** — FFT visualization with multiple color maps
- **Annotations** — Time-synced text with drag-to-edit

## Choose your integration

### React

For React 18+ / 19+ projects. Hooks + components.

```tsx
const { tracks, loading } = useAudioTracks([{ src: '/drums.mp3' }]);
return (
  <WaveformPlaylistProvider tracks={tracks}>
    <Waveform />
  </WaveformPlaylistProvider>
);
```

[**→ Get started with React**](/docs/getting-started/installation)

### Web Components

For any framework or vanilla HTML. Custom elements; no React required.

```html
<daw-editor>
  <daw-track src="/drums.opus" name="Drums"></daw-track>
  <daw-track src="/bass.opus" name="Bass"></daw-track>
</daw-editor>
```

[**→ Get started with Web Components**](/docs/web-components/getting-started)

## Architecture at a glance

Both integration paths share the same headless engine (`@waveform-playlist/engine`) and a pluggable `PlayoutAdapter` for audio backends. React wraps the engine in providers + hooks; Web Components wrap it in Lit-based custom elements (`@dawcore/components`).

- [API reference](/docs/api/llm-reference) — full TypeScript surface
- [Examples](/examples) — runnable demos
````

- [ ] **Step 3: Run the build to verify nothing broke**

Run:
```bash
pnpm --filter website build 2>&1 | tail -15
```

Expected: `[SUCCESS] Generated static files in "build".` The project sets `onBrokenLinks: 'warn'` in `docusaurus.config.ts`, so the broken `/docs/web-components/getting-started` link (page doesn't exist until Task 2) will print a warning but not fail the build. The other three links (`/docs/getting-started/installation`, `/docs/api/llm-reference`, `/examples`) must resolve cleanly — any warnings about those are real bugs to fix here.

Do not commit between tasks — this is one atomic Phase-1 commit at the end.

---

## Task 2: Create the `web-components/` section + `getting-started.md`

**Files:**
- Create: `website/docs/web-components/_category_.json`
- Create: `website/docs/web-components/getting-started.md`

- [ ] **Step 1: Create the directory**

Run:
```bash
mkdir -p website/docs/web-components
```

- [ ] **Step 2: Create `_category_.json`** so the sidebar entry has a clean label and points directly at `getting-started.md` (no auto-generated index page in between)

Write `website/docs/web-components/_category_.json`:

```json
{
  "label": "Web Components",
  "position": 3,
  "link": {
    "type": "doc",
    "id": "web-components/getting-started"
  }
}
```

(`position: 3` sits the section after Intro and Getting Started in the sidebar; adjust during Step 4 verification if the resulting order looks wrong.)

- [ ] **Step 3: Write `getting-started.md`**

Write `website/docs/web-components/getting-started.md` with this exact content:

````markdown
---
sidebar_position: 1
description: "Quick-start guide for @dawcore/components — install, set up, render your first multitrack editor."
---

# Getting Started with Web Components

`@dawcore/components` ships framework-agnostic Web Components for multi-track audio editing. Drop `<daw-editor>` into any HTML page — no React, no build step required.

## Installation

```bash
npm install @dawcore/components
```

Peer dependencies:

```bash
npm install @waveform-playlist/core @waveform-playlist/engine
```

Audio backend — choose one:

```bash
npm install @dawcore/transport          # Native Web Audio (recommended)
# or
npm install @waveform-playlist/playout tone  # Tone.js (required for MIDI playback today)
```

Optional (for recording):

```bash
npm install @waveform-playlist/worklets
```

## Minimal setup

```html
<script type="module">
  import '@dawcore/components';
  import { NativePlayoutAdapter } from '@dawcore/transport';

  const editor = document.querySelector('daw-editor');
  editor.adapter = new NativePlayoutAdapter(new AudioContext());
</script>

<daw-editor id="editor" samples-per-pixel="1024" wave-height="100" timescale>
  <daw-track src="/audio/drums.opus" name="Drums"></daw-track>
  <daw-track src="/audio/bass.opus" name="Bass"></daw-track>
  <daw-track src="/audio/synth.opus" name="Synth"></daw-track>
</daw-editor>

<daw-transport for="editor">
  <daw-play-button></daw-play-button>
  <daw-pause-button></daw-pause-button>
  <daw-stop-button></daw-stop-button>
</daw-transport>
```

The editor loads audio, generates waveforms, and handles playback.

## Important: AudioContext + adapter are required

`<daw-editor>` does **not** create an `AudioContext` for you. You must:

1. Create an `AudioContext` (`new AudioContext()` or `new AudioContext({ sampleRate: 48000 })`)
2. Instantiate a `PlayoutAdapter` (e.g., `NativePlayoutAdapter` from `@dawcore/transport`)
3. Assign it to `editor.adapter`

This is intentional — it lets consumers choose their audio backend (native Web Audio or Tone.js) and keeps `AudioContext` lifecycle in your hands.

## Try it locally

Two runnable starter apps live in the repo:

```bash
git clone https://github.com/naomiaro/waveform-playlist.git
cd waveform-playlist
pnpm install
pnpm example:dawcore-native    # Native Web Audio backend
pnpm example:dawcore-tone      # Tone.js backend
```

Each starter has multiple example pages — basic, multiclip, recording, spectrogram, MIDI, beats grid, metronome — you can use as a starting point.

## Next steps

For the complete element catalog, audio-backend comparison, MIDI loading, recording reference, and CSS theming, see the [`@dawcore/components` README](https://github.com/naomiaro/waveform-playlist/blob/main/packages/dawcore/README.md).

Using React instead? → [React Getting Started](/docs/getting-started/installation)
````

- [ ] **Step 4: Build the site and confirm the new page resolves**

Run:
```bash
pnpm --filter website build 2>&1 | tail -15
```

Expected: `[SUCCESS] Generated static files in "build".` No broken-link errors. The intro.md's `/docs/web-components/getting-started` link now resolves.

Then confirm the file landed at the expected route:

```bash
ls website/build/docs/web-components/
```

Expected: a `getting-started/` directory containing `index.html`.

---

## Task 3: Smoke test in a browser, then commit and push

**Files:** None modified in this task.

- [ ] **Step 1: Start the serve process in the background**

Run:
```bash
pnpm --filter website serve &
```

Wait ~3 seconds for it to boot, then proceed.

- [ ] **Step 2: Manual smoke test — open six URLs in order and confirm each**

Open in a browser:

| # | URL | Expected |
|---|---|---|
| 1 | `http://localhost:3000/waveform-playlist/docs/` | New intro page — title "Waveform Playlist", "What it does" bullet list, "Choose your integration" section with React + WC subsections |
| 2 | Click **"Get started with Web Components"** | Lands on `/docs/web-components/getting-started` |
| 3 | Confirm WC page | Title "Getting Started with Web Components"; Installation section; Minimal setup HTML example; "Important: AudioContext + adapter are required" callout; "Try it locally" section |
| 4 | Navigate back, click **"Get started with React"** | Lands on `/docs/getting-started/installation` |
| 5 | Confirm React page unchanged | The existing React onboarding page renders exactly as before (no regression) |
| 6 | Open the sidebar | "Web Components" section appears in the sidebar tree; clicking it goes to the getting-started page (not an auto-generated index) |

If anything in this table fails: stop, fix, re-build, re-verify. Don't commit until all six pass.

- [ ] **Step 3: Stop the serve process**

Run:
```bash
kill %1
```

Or `Ctrl+C` if running in the foreground.

- [ ] **Step 4: Stage and commit**

Run:
```bash
git add website/docs/intro.md website/docs/web-components/_category_.json website/docs/web-components/getting-started.md
git commit -m "feat(website): land Web Components onboarding path (IA phase 1)

Per docs/specs/2026-05-24-ia-restructure-design.md — phase 1 of the IA
restructure.

- Rewrite intro.md as a framework-picker landing page. React and Web
  Components each get a clearly-labeled section with a code preview and
  a 'Get started' link.
- Add /docs/web-components/ section. Autogenerated sidebar picks up the
  new directory; _category_.json sets the label and points the category
  link directly at getting-started.md (no auto-index page).
- getting-started.md is grounded in the existing packages/dawcore/README
  Quick Start content (install, minimal setup, AudioContext + adapter
  callout, link to standalone Vite apps for clone-and-run).

No existing URLs change. No redirects needed. React onboarding at
/docs/getting-started/installation is unchanged."
```

- [ ] **Step 5: Push** (`docs/ia-restructure-spec` is the branch and PR #398 updates automatically)

Run:
```bash
git push
```

---

## What this plan deliberately does NOT do

These belong to subsequent phase plans, each written when the prior phase ships:

- **Phase 2 — Concepts content.** Write the 9 framework-agnostic `Concepts/*.md` pages.
- **Phase 3 — React reorganization.** Move existing React-specific guides into a structured `react/` section with cross-links to Concepts.
- **Phase 4 — WC content.** Write the other 16 WC integration pages (editor-element, visual-elements, recording, midi, etc.) listed in the spec.
- **Phase 5 — Examples reorg.** Reorganize `/examples` landing page by framework; port WC examples from `examples/dawcore-{native,tone}/` as Docusaurus-native React pages. Requires source-aliasing `@dawcore/*` in `docusaurus.config.ts`.
- **Phase 6 — Cleanup.** Delete old `/docs/guides/*.md` pages once superseded; install `@docusaurus/plugin-client-redirects` and add redirect map for moved URLs; update `llms.txt` + `llm-reference.md` to match the new structure.

Splitting by phase keeps each PR a tight unit of review and lets later plans benefit from execution learnings from earlier ones.

---

## Self-review checklist

After implementing all three tasks:

- [ ] `pnpm --filter website build` exits clean (no broken-link errors, no MDX warnings on the changed/new files)
- [ ] Both new and old onboarding paths work end-to-end in a live browser (the six-URL smoke test)
- [ ] No existing pages were removed
- [ ] No `sidebars.ts` edits (autogenerated sidebar picks up the new directory automatically)
- [ ] No `docusaurus.config.ts` edits (no plugin install, no source aliases needed for static markdown)
- [ ] Commit message references the design spec for traceability
- [ ] Branch pushed; PR #398 reflects the new commit
